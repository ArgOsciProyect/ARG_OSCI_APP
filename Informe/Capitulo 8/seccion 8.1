8.1 Procesamiento inicial de datos
El procesamiento comienza con la extracción de los datos que llegan por el socket TCP. Se implementó un sistema de buffering circular cuyo tamaño viene definido por el parámetro samplesPerPacket en la configuración del dispositivo. Los datos llegan en paquetes de 16 bits donde cada muestra contiene tanto el valor del ADC como información del canal.
Para extraer los valores útiles se implementaron máscaras de bits precalculadas que vienen definidas en la configuración. Las máscaras permiten separar el valor del ADC y el canal en una única operación, evitando múltiples operaciones de desplazamiento. Los valores de las máscaras se calculan una única vez durante la inicialización para optimizar el rendimiento.
El procesamiento de las muestras se realiza según un factor divisor también configurado desde el dispositivo. Se utiliza una operación módulo sobre el índice de cada muestra para determinar cuáles se procesan y cuáles se descartan. 
La transformación a coordenadas físicas se realiza utilizando los parámetros maxBits y midBits de la configuración para convertir los valores crudos a voltajes. El valor medio configurado se usa como referencia para el cero, permitiendo representar señales tanto positivas como negativas. La coordenada temporal se calcula de forma acumulativa basándose en el período de muestreo efectivo que considera el factor divisor aplicado.
Todo este procesamiento se ejecuta en un isolate dedicado que opera de forma asíncrona respecto al hilo principal. La implementación según el patrón Pipeline permite que cada etapa del procesamiento sea independiente y modificable sin afectar al resto. Siguiendo los principios de Clean Architecture, la lógica de procesamiento se mantiene desacoplada de la interfaz de usuario y de los detalles de implementación del hardware. 
El uso de isolates permite aprovechar múltiples núcleos del procesador y evita que el procesamiento afecte la responsividad de la interfaz. La comunicación entre isolates se realiza mediante puertos de envío y recepción, manteniendo el principio de responsabilidad única al separar la recepción de datos del procesamiento.
