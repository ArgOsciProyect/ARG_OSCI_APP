import 'package:flutter/material.dart';

class SetupPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Setup Page'),
      ),
      body: Center(
        child: Text('Welcome to the Setup Page!'),
      ),
    );
  }
}